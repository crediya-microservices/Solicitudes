server:
  port: 8082
spring:
  application:
    name: "Solicitudes"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"
routes:
  paths:
    requests: "/api/v1/solicitud"
#adapters:
#  r2dbc:
#    host: ${SPRING_R2DBC_HOST:solicitudes-db}
#    port: ${SPRING_R2DBC_PORT:5432}
#    database: ${SPRING_R2DBC_DATABASE:solicitudes}
#    schema: ${SPRING_R2DBC_SCHEMA:public}
#    username: ${SPRING_R2DBC_USERNAME:postgres}
#    password: ${SPRING_R2DBC_PASSWORD:postgres}

adapters:
  r2dbc:
    host: "localhost"
    port: 5432
    database: "solicitudes"
    schema: "public"
    username: "postgres"
    password: "postgres"
services:
  auth:
    base-url: http://localhost:8081

    endpoints:
      get-user: "/api/v1/usuarios/"

jwt:
  secret: "Aa5592770*1234567890abcdef12345678"
  expiration-ms: 3600000